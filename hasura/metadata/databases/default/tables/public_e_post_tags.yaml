table:
  name: e_post_tags
  schema: public
is_enum: true
select_permissions:
  - role: anonymous
    permission:
      columns:
        - type
      filter: {}
  - role: user
    permission:
      columns:
        - type
      filter: {}
